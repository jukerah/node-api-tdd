generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Products {
  productId   String @id
  name        String
  description String
  price       Int
  amount      Int
}
